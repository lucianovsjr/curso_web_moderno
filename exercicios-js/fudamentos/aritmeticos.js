/*
    Operadores binarios. Dois operando.
*/
const [a, b, c, d] = [3, 5, 1, 15];

const soma = a + b + c + d;
console.log(soma);

const subtracao = d - b;
const multiplicacao = a * b;
const divisao = d / a;
const modulo = a % 2; // utilziado para saber e par ou impa

console.log(subtracao, multiplicacao, divisao, modulo);

// simbolo de menos e um operador unario
console.log(-subtracao);